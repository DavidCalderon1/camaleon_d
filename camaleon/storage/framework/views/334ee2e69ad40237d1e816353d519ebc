<table class="table table-responsive" id="cuentaGrupos-table">
    <thead>
        <th>Nombre</th>
        <th>Descripcion</th>
        <th>Ajuste</th>
        <th>Cntg Cntcid</th>
        <th colspan="3">Action</th>
    </thead>
    <tbody>
    <?php foreach($cuentaGrupos as $cuentaGrupo): ?>
        <tr>
            <td><?php echo $cuentaGrupo->nombre; ?></td>
            <td><?php echo $cuentaGrupo->descripcion; ?></td>
            <td><?php echo $cuentaGrupo->ajuste; ?></td>
            <td><?php echo $cuentaGrupo->cntg_cntcid; ?></td>
            <td>
                <?php echo Form::open(['route' => ['cuentaGrupos.destroy', $cuentaGrupo->cntg_id], 'method' => 'delete']); ?>

                <div class='btn-group'>
                    <a href="<?php echo route('cuentaGrupos.show', [$cuentaGrupo->cntg_id]); ?>" class='btn btn-default btn-xs'><i class="glyphicon glyphicon-eye-open"></i></a>
                    <a href="<?php echo route('cuentaGrupos.edit', [$cuentaGrupo->cntg_id]); ?>" class='btn btn-default btn-xs'><i class="glyphicon glyphicon-edit"></i></a>
                    <?php echo Form::button('<i class="glyphicon glyphicon-trash"></i>', ['type' => 'submit', 'class' => 'btn btn-danger btn-xs', 'onclick' => "return confirm('Are you sure?')"]); ?>

                </div>
                <?php echo Form::close(); ?>

            </td>
        </tr>
    <?php endforeach; ?>
    </tbody>
</table>