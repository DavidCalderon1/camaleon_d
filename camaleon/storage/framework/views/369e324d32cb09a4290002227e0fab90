<table class="table table-responsive" id="cuentas-table">
    <thead>
        <th>Nombre</th>
        <th>Descripcion</th>
        <th>Ajuste</th>
        <th>Cnt Cntgid</th>
        <th colspan="3">Action</th>
    </thead>
    <tbody>
    <?php foreach($cuentas as $cuenta): ?>
        <tr>
            <td><?php echo $cuenta->nombre; ?></td>
            <td><?php echo $cuenta->descripcion; ?></td>
            <td><?php echo $cuenta->ajuste; ?></td>
            <td><?php echo $cuenta->cnt_cntgid; ?></td>
            <td>
                <?php echo Form::open(['route' => ['cuentas.destroy', $cuenta->cnt_id], 'method' => 'delete']); ?>

                <div class='btn-group'>
                    <a href="<?php echo route('cuentas.show', [$cuenta->cnt_id]); ?>" class='btn btn-default btn-xs'><i class="glyphicon glyphicon-eye-open"></i></a>
                    <a href="<?php echo route('cuentas.edit', [$cuenta->cnt_id]); ?>" class='btn btn-default btn-xs'><i class="glyphicon glyphicon-edit"></i></a>
                    <?php echo Form::button('<i class="glyphicon glyphicon-trash"></i>', ['type' => 'submit', 'class' => 'btn btn-danger btn-xs', 'onclick' => "return confirm('Are you sure?')"]); ?>

                </div>
                <?php echo Form::close(); ?>

            </td>
        </tr>
    <?php endforeach; ?>
    </tbody>
</table>